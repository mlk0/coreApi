{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/hotels.api/hotels.api.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}